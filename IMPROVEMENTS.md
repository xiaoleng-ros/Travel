# 改进台账 · IMPROVEMENTS

> 本文档记录「人间快照」的改进项：**做了什么、改到哪一步、怎么验证的、哪些还没做**。
> 每次改动后追加/更新，便于随时对照当前代码状态。
>
> 最近更新：2026-09-22（修复七牛签名 base64 填充缺陷；函数部署区改东京；另附推进状态审查，
> 见文末第十至十三节）
> 上一轮：2026-09-21（第 4 轮：完成 EdgeOne 全 Serverless 改造，见第九节）

---

## 一、本轮改动总览

| # | 类别 | 改动 | 状态 | 验证方式 |
|---|------|------|------|----------|
| 1 | 安全 | 改密码后旧会话立即失效 | ✅ 已完成 | 端到端验证：旧 Cookie 返回 401 |
| 2 | 安全 | 修复 token 黑名单永不清理 | ✅ 已完成 | 启动迁移日志 + 数据库字段检查 |
| 3 | 运维 | 备份轮转 + 支持异地目录 | ✅ 已完成 | 端到端验证：生成带时间戳归档 |
| 4 | 性能 | 公开接口增加缓存头 | ✅ 已完成 | 端到端验证：响应头含 max-age |
| 5 | 功能 | 读取 EXIF 拍摄时间并落库 | ✅ 已完成 | 单元测试 + 端到端验证 |
| 6 | 性能 | 缩略图转 WebP、长边 480→640 | ✅ 已完成 | 单元测试 + 端到端验证 |
| 7 | 运维 | 新增 `/healthz` 健康检查 | ✅ 已完成 | 端到端验证：返回 status ok |
| 8 | 体验 | 新增 404 兜底页 | ✅ 已完成 | 前端构建通过 |
| 9 | 工程 | 新增 GitHub Actions CI | ✅ 已完成 | 配置已就位，待推送后触发 |
| 10 | **缺陷** | 中文文件名无法上传 | ✅ 已完成（顺手发现并修复） | 端到端验证：中文名上传成功 |
| 11 | **缺陷** | Windows 上临时文件残留 | ✅ 已完成（顺手发现并修复） | 端到端验证：上传后无 .tmp |
| 12 | **缺陷** | 七牛 SDK 故障会导致服务进程崩溃 | ✅ 已完成（影响原有上传路径） | 真实故障复现 + 10 项断言全部通过 |
| 13 | 加固 | 七牛 Region 支持，跳过区域查询 | ✅ 已完成 | 日志对比：错误由「区域查询失败」变为直接上传 |
| 14 | 运维 | 数据库快照可同步备份到对象存储 | ✅ 已完成（默认关闭） | 端到端验证：失败不影响服务与本地写入 |
| 15 | **缺陷** | 删除封面照片后相册封面变死链 | ✅ 已完成 | 端到端验证：封面正确回退且文件存在 |
| 16 | **缺陷** | scrypt 每次重算，118ms 同步阻塞 | ✅ 已完成 | 实测 118.77ms → 2.30ms（51 倍） |
| 17 | 安全 | 公开接口泄漏 storage_key 等内部字段 | ✅ 已完成 | 端到端验证：内部字段为 0 |
| 18 | 安全 | 公开接口可从根路径绕开限流 | ✅ 已完成 | 端到端验证：根路径返回 404 |
| 19 | **缺陷** | 存储配置页加载失败整页白屏 | ✅ 已完成 | 构建通过 |
| 20 | **缺陷** | 灯箱关闭定时器竞态 | ✅ 已完成 | — |
| 21 | **缺陷** | 滚动监听反复重绑定 + 帧取消无效 | ✅ 已完成 | — |
| 22 | **缺陷** | 删除相册失败时静默无反馈 | ✅ 已完成 | — |
| 23 | **缺陷** | 401 处理不完整；登录页输错密码会整页刷新 | ✅ 已完成 | — |
| 24 | 可访问性 | 照片卡片键盘完全不可操作 | ✅ 已完成 | — |

**验证结果**：后端单元测试 16/16 · 前端单元测试 19/19 · 端到端检查 18/18 · 云端备份专项 10/10 · 本轮修复专项 11/11 · 前端构建成功。
（2026-09-22 复核：前端测试实为 **20/20**——本节写就后 `tests/upload.test.js` 又在 `adb1741` 中补了一条用例，文档未同步。）

---

## 二、逐项说明

### 1. 改密码后旧会话立即失效（安全 · 重要）

**问题**：原实现只把「当前这一个」token 拉黑。在电脑上改完密码后，手机上、或已被窃取的 token 在 7 天有效期内仍能进后台。

**改动**：
- `server/db.js` — 新增 `changeUserPassword()`，修改密码时写入 `password_changed_at`（秒级时间戳）
- `server/middleware/auth.js` — 鉴权时比对 token 的 `iat` 与 `password_changed_at`，早于则拒绝
- `server/routes/auth.js` — 改密码改走 `changeUserPassword()`，并保留原有黑名单逻辑覆盖同秒边界

**效果**：修改密码 = 所有设备强制下线。老数据无 `password_changed_at` 时跳过校验，不影响现有登录状态。

### 2. token 黑名单永不清理（安全）

**问题**：`db.json` 里 `tokenBlacklist` 存的是旧版纯字符串（无过期时间），清理函数对这类条目只能「保守保留」，导致它随每次登录登出无限增长。

**改动**：`server/db.js` 新增 `migrateTokenBlacklist()`，启动时一次性升级为 `{ jti, exp }` 格式并给 7 天宽限期——期间旧 token 仍被正常拦截，到期自动回收。

### 3. 备份轮转 + 支持异地目录（运维 · 重要）

**问题**：原先只保留一份 `db.backup.json`，且与主库同目录同磁盘。磁盘损坏或服务器回收时，主库和备份一起没。

**改动**：`server/db.js`
- 保留 `db.backup.json`（供启动时快速恢复损坏的主库，逻辑不变）
- 额外写入带时间戳的归档 `db-<ISO时间>.json`，按份数自动轮转
- 新增环境变量 `BACKUP_DIR`（可指向另一块磁盘或云盘挂载点）、`BACKUP_KEEP`（保留份数，默认 20）

> ⚠️ **还需你手动做一步**：把 `BACKUP_DIR` 指到真正的异地路径，否则归档仍在同一块盘上。

### 4. 公开接口缓存（性能）

`server/routes/public.js` 为 `/api/album/public/*` 加上 `Cache-Control: public, max-age=30, stale-while-revalidate=120`（Express 已自动生成 ETag，命中返回 304）。
取 30 秒是权衡：后台刚上传的照片最多延迟 30 秒出现在前台。

### 5. EXIF 拍摄时间（功能 · 重要）

**问题**：原先 `create_time` 用的是**上传时间**，回忆录按上传时间排序没有意义。

**改动**：
- `server/utils/image.js` — 新增 `readCaptureTime()`，读 `DateTimeOriginal`
- `server/routes/photos.js` — 上传时写入 `taken_at`
- `server/db.js` / `server/routes/public.js` — 排序与展示优先用 `taken_at`，缺失时回退上传时间

> **一个容易踩的坑**：EXIF 时间字符串不带时区，用 exifr 默认解析会当成 UTC，东八区拍的照片会早 8 小时。实现里用 `reviveValues:false` 取原始字符串自行转换，全程不做时区换算。`tests/image.test.js` 有专门的回归防线。

**老照片怎么办**：历史数据没有 `taken_at`，展示与排序行为与改动前完全一致。若想让老照片也用拍摄时间，需要重新读取原图 EXIF（原图默认保留在 `KEEP_ORIGINAL` 下）。

### 6. 缩略图转 WebP（性能）

`server/utils/image.js` — 缩略图统一输出 WebP（quality 78），长边 480→640（适配高分屏）。
PNG 照片的缩略图原先动辄几百 KB，转 WebP 后通常小六成以上，直接降低 CDN 流量。

> 注意：`processThumbnail` 现在返回 `{ buffer, ext }`，扩展名不再等于原图格式。**已有历史数据的缩略图仍是原格式**，两种格式可共存，无需迁移。

### 7 & 8. 健康检查与 404 页

- `server/index.js` — `GET /healthz` 返回运行状态，挂在 `/api` 之外以免占用限流额度，可配 UptimeRobot 做免费监控
- `src/components/NotFound.jsx` + `src/App.jsx` — 未匹配路径落到 404 页，不再白屏

### 9. CI

`.github/workflows/ci.yml` — push/PR 到 `main` 时自动跑前端测试+构建、后端测试。首次推送后到 GitHub Actions 页面确认绿灯。

### 10. 中文文件名无法上传（缺陷修复 · 重要）

**问题**：multer 把 UTF-8 文件名按 latin1 解码，`海边日落.jpg` 变成乱码后被文件名校验判为「非法字符」直接拒收。
项目里本来就有 `fixEncoding()` 处理这个问题，但**校验发生在它之前**，所以手机上导出的中文名照片一律传不上来。

**改动**：`server/routes/photos.js` 的 `fileFilter` 先还原编码再校验，并把修复后的名字写回 `file.originalname`。

> 遗留边界：日文、韩文等非中文的非 ASCII 文件名仍会被拒（`fixEncoding` 只认中文字符范围）。如需要可再放宽。

### 11. Windows 上临时文件残留（缺陷修复）

**问题**：上传时的 `.tmp` 临时文件在 Windows 上删不掉——`sharp` 与 `exifr` 直接读文件路径时会持有句柄，`unlinkSync` 抛 `EPERM` 被 `catch {}` 静默吞掉，只能等下次启动时兜底清理。

**改动**：
- `server/utils/image.js` — 改为一次性读入 Buffer，让 `sharp` 与 `exifr` 只在内存中工作，不再接触文件
- `server/routes/photos.js` — 清理改用带重试的 `rmSync`，且失败时输出日志而非静默吞掉

### 12. 七牛 SDK 故障会导致服务进程崩溃（缺陷修复 · 严重）

**问题**：七牛 SDK 在「查询存储区域」失败时（凭证失效、网络抖动、服务端异常），
会抛出**它自己回调风格 API 没能接住的 Promise 拒绝**。Node 15+ 对未处理的 Promise 拒绝的
默认行为是**直接终止进程**——表现为「某一次上传失败后，整个服务再也打不开」。

**影响范围**：这不是新引入的问题，**现有的照片上传路径同样会触发**。
在没有云端备份功能之前，只要七牛凭证过期或区域查询偶发失败，服务器就会崩溃。

**复现与确认**：用无效凭证触发，进程确实退出；服务日志中可见
`Error: Query regions failed with HTTP Status Code 612` 后进程终止。

**改动**：
- `server/index.js` — 新增 `process.on('unhandledRejection')` 兜底。
  对相册服务而言，「记录日志并继续提供服务」明显优于「直接崩溃」，
  而 Node 的默认行为是为了让开发者发现 bug，不适合生产环境
- `server/storage/providers.js` — 见下一节，从根上减少触发概率

### 13. 七牛 Region 支持，跳过区域查询（加固）

七牛 SDK 的 `Config.zone` 一旦显式指定，就会直接构造 Region 而**不再请求 `/v4/query`**
（见 `qiniu/conf.js` 的 `getRegionsProviderFromZone`）。

**改动**：
- `server/storage/providers.js` — 新增 `createKodoConfig()`，把用户填写的区域
  （`z0` / `cn-east-1` / `华东` 等写法均可）归一化后映射到 SDK 的 Zone 类，
  应用于上传、删除、连通性测试三处
- `server/routes/storage.js` — 配置项标签改为「Region（建议填写）」，并给出区域对照

**效果**（实测日志对比）：

| 场景 | 报错 | 是否查询区域 |
|------|------|--------------|
| 不填 Region | `Query regions failed...` | 是，且会触发崩溃路径 |
| 填写 Region | `bad token`（直接上传被拒） | 否，已跳过 |

> 未填 Region 时功能仍可用，只是每次上传多一次区域查询、并有触发崩溃路径的风险，
> 此时由 12 中的进程级兜底保证服务不退出。

### 14. 数据库快照可同步备份到对象存储（运维 · 默认关闭）

**动机**：照片本体在云上，但「哪张照片属于哪个相册、标题、拍摄时间」全在 db.json 里。
db.json 一丢，云端照片就成了无法归类的无名文件——**元数据才是真正的单点**。

**改动**：`server/db.js` 新增 `uploadBackupToCloud()`
- 每次写入上传 `<前缀>db-latest.json`（覆盖）；每天首次写入额外上传一份 `db-YYYY-MM-DD.json`
- 上传不阻塞本地写入，失败只记日志，绝不影响数据落盘
- 云端清理交给对象存储的生命周期规则，程序不需要 list/delete 权限

**默认关闭**，需显式设置 `BACKUP_TO_CLOUD=true`
（原因见上方安全说明：bucket 若为公开读，备份会被任何人下载）。

**验证**：10/10 断言通过——含「进程不崩溃」「失败被捕获」「数据完整性」「配置保存接口正常」。

### 15. 删除封面照片后相册封面变死链（缺陷修复）

**问题**：上传时封面存的是**缩略图**（`cover: added[0].thumb || added[0].url`），
而删除时却只拿**主图 URL** 比对（`album.cover === photo.url`）——两者永远不相等，
封面重置逻辑从来没生效过。

**实测确认**：上传两张 → 封面 = 第一张的 `.webp` 缩略图 → 彻底删除第一张后，
封面仍指向那个 `.webp`，而**该文件已经被删掉了**。前台加载相册封面直接 404。

**改动**：`server/routes/photos.js`
- `resetAlbumCoverIfNeeded()` 改为接收照片对象，对 `url` / `thumb` / `original_url` 全部比对
- 单张删除、批量删除都走同一个函数
- **彻底删除处额外补了一次兜底**：那是物理文件真正被删的环节，
  万一软删除阶段没重置成功（例如改动前遗留的历史数据），封面就会永久指向不存在的文件

**验证**：端到端确认——删除封面照片后封面正确回退到剩余照片，且文件存在。

### 16. scrypt 每次重算，同步阻塞 118ms（缺陷修复 · 性能）

**问题**：`utils/crypto.js` 的 `getKey()` 每次加解密都重跑 `scryptSync`。
scrypt 是**刻意设计成慢的**（防暴力破解），实测单次 118ms。
而云端存储模式下，每张照片要触发多次解密（主图 / 原图 / 缩略图各一次 `saveFile`），
批量上传 20 张就是**数秒的累计开销**。更糟的是 `scryptSync` 是同步的——
这几秒里整个 Node 进程无法响应任何请求。

**实测数据**（改造前后对比，同一台机器）：

| 指标 | 修复前 | 修复后 |
|------|--------|--------|
| 单次加解密 | 118.77 ms | **2.30 ms** |
| 提速 | — | **约 51 倍** |

**改动**：`server/utils/crypto.js` — 模块级缓存派生密钥，仅在密钥字符串变化时重新派生。

> 注意：这个问题的触发前提是用云端存储。你的图片全在七牛，所以**一定会遇到**。

### 17. 公开接口泄漏内部字段（安全）

**问题**：`routes/public.js` 用 `...p` 把整条照片记录返回给未鉴权访客，
包含 `storage_key`、`thumb_key`、`original_key`、`storage_provider`、`sort_order`。
拿到 key 就能绕过应用直接拼出对象存储地址，在 bucket 公开读时可用来枚举原图。

**改动**：新增 `PUBLIC_PHOTO_FIELDS` 白名单 + `toPublicPhoto()`，
只保留前台展示真正需要的 12 个字段（含灯箱要用的 `original_url`）。

**验证**：端到端确认内部字段为 0，且前台所需字段完整。

### 18. 公开接口可从根路径绕开限流（安全）

**问题**：`index.js` 里 `publicRoutes` 同时挂在 `/api` 和根路径，
而 `apiLimiter` 只作用于 `/api`。因此 `/album/public/list` 从根路径访问**完全不受限流**，
可以无限调用——而这两个接口每次请求都要全量遍历照片并排序，是典型的 CPU 放大攻击面。

**改动**：
- `routes/public.js` 拆成 `publicRouter`（公开接口）与 `uploadRouter`（图片）分别导出
- `index.js` — 公开接口只挂 `/api`（走限流）；图片单独挂根路径且不限流
  （瀑布流一屏就请求几十张图，限流反而会误伤）

**验证**：端到端确认根路径返回 404、`/api` 正常、`/uploads` 图片访问未受影响。

### 19. 存储配置页加载失败整页白屏（缺陷修复）

`admin/StorageSettings.jsx` 的 `settings` 初值为 `null`，`loadData` 失败时只弹提示不兜底，
紧接着渲染就去读 `settings.activeProvider` → 抛 TypeError。
而后台**没有 ErrorBoundary**，结果是整页白屏、且用户没有任何重试入口。

**改动**：加载完成但 `settings` 仍为空时，渲染错误提示 + 「重新加载」按钮。

### 20. 灯箱关闭定时器竞态（缺陷修复）

`AlbumListPage.jsx` 的 `closeLightbox` 里有一个 300ms 的 `setTimeout`，从不清理。
**触发场景**：关闭灯箱后 300ms 内点开另一张照片，上一个定时器会把刚打开的灯箱清成 `null`，
表现为「灯箱闪一下就没了」。

**改动**：用 `closeTimerRef` 持有定时器，打开时先取消旧的，组件卸载时也清理。

### 21. 滚动监听反复重绑定（缺陷修复 · 性能）

`PhotoGrid.jsx` 的滚动 effect 依赖 `onReachEnd`，而父组件每次渲染都会传入新的函数引用，
导致每次渲染都解绑/重绑滚动监听、并立即触发一次触底检测，滚动过程中可能重复请求。
另外 `cancelAnimationFrame(0)` 是个无效调用——rAF 的 id 从未被保存。

**改动**：用 ref 持有最新的 `onReachEnd`（依赖数组移除它），并正确保存/取消 rAF id。

### 22. 删除相册失败时静默无反馈（缺陷修复）

`AlbumManage.jsx` 的 `handleDelete` 没有 `try/catch`。接口失败时 Promise 被拒绝，
弹窗既不关闭也不给任何提示——而**删除相册会连带删掉其下所有照片**，
用户很容易误以为已经删掉了。

**改动**：加 `try/catch/finally`，弹窗内显示错误信息，删除中禁用按钮防重复提交。

### 23. 401 处理不完整，且登录页输错密码会整页刷新（缺陷修复）

`api/real.js` 的 401 拦截器有两个问题：
1. 只清 `localStorage`，不清 `sessionStorage` 的 `admin_logged_in`。
   而 `RequireAuth` **优先读这个标记并直接放行** —— 会话失效后重新进入 `/admin`
   仍会被放进后台，紧接着每个接口都 401、页面反复跳转。
2. 无条件 `window.location.href = '/admin/login'`。而**登录失败（密码错误）同样返回 401**，
   于是用户在登录页输错密码会导致整页刷新、刚输入的内容全部丢失。

**改动**：同时清除两个存储标记；仅在不在登录页时才跳转，并改用 `replace`。

### 24. 照片卡片键盘完全不可操作（可访问性）

`PhotoItem.jsx` 的照片卡片是 `motion.div` + `onClick`，没有 `role` / `tabIndex` / `onKeyDown`。
**键盘用户完全无法打开灯箱——也就是前台最核心的交互**。

**改动**：加 `role="button"` + `tabIndex={0}` + Enter/Space 处理 + 主题适配的 focus 样式。

---

> 本轮采用了与之前不同的方法：先并行做了两轮深度审计（前端 / 后端），
> 再**逐条实测验证**后才动手。前两轮的教训是——光读代码发现不了真问题，
> 中文文件名、`.tmp` 残留、七牛崩溃、封面死链这几条都是实际跑出来的。

---

## 三、未做的项（及原因）

### 产品升级项

| 项 | 状态 | 说明 |
|----|------|------|
| HEIC 支持 | 🚫 **已决策不做**（2026-09-21） | iPhone 默认拍照格式。不做后，从 iPhone 导入的照片需先在手机上转成 JPEG 再上传。若日后改变主意，低成本变体是**前端转换**（`heic2any` 在浏览器里转码后再上传），无需后端支持 HEIC 解码 |
| 照片搜索 | 🚫 已决策不做 | |
| 分享链接 | 🚫 已决策不做 | |
| GPS 足迹地图 | 🚫 已决策不做 | |
| 动态 OG 预览 | 🚫 已决策不做 | |
| TOTP 两步验证 | 🚫 已决策不做 | |
| 迁移数据库 | 🚫 **已评估：暂不迁移** | 见文末「存储方案评估」——实测 5000 张照片仍无需迁移 |

### 工程与健壮性项（本轮审计发现，未修）

| 项 | 影响 | 建议 |
|----|------|------|
| 存储配置缺必填校验 | `PUT /api/admin/storage` 不校验 bucket/region/key，存错配置后批量上传整批失败，前端只看到「没有成功处理任何图片」 | 加必填校验，并区分「单张跳过」与「系统性错误」原样上报 |
| 上传失败可能留下孤儿文件 | 云端已上传成功但 `db.addPhotos` 落盘失败时，对象无任何记录可追溯 | 落盘失败时回滚已上传的对象，或记录待清理清单 |
| 上传内存放大 | 并发 4 且每张整文件读入内存，同时持有原图+处理图+缩略图，多请求并发可推高常驻内存数百 MB | 限制上传并发总数，或改为流式处理 |
| 云存储客户端不复用 | 每次 `saveFile`/`deleteFile` 都 `new OSS/COS`，20 张上传可新建数十个客户端 | 按 provider 缓存客户端实例 |
| 回收站无限增长 | 无自动清空与批量彻底删除，软删除记录与物理文件永久累积 | 加保留期策略与批量彻底删除接口 |
| `scripts/backfill-thumbs.js` 与线上不一致 | 直接 `writeFileSync` 覆盖 db.json（中断即损坏），且输出 480px 而线上已统一 640px WebP | 改用原子写入，并同步到 640px WebP |
| 登录限流为内存 + 纯 IP 维度 | 重启即清零；多实例各自计数；反代与 Node 不同机时全员共用反代 IP（一人失败 5 次封全站） | 增加按账号维度，必要时接持久化存储 |
| 后台灯箱无键盘支持 | Esc / ← / → 只在前台注册，后台预览无按键处理，按钮也无 `aria-label` | 把键盘监听到移进 `Lightbox` 组件内部 |
| 拖拽排序触屏不可用 | 依赖 HTML5 `draggable`，触屏不产生 drag 事件；且按索引定位，拖拽中数据变化会写错顺序 | 改用 pointer 事件，并按 id 定位 |
| 移动端隐藏按钮点不到 | 编辑/删除按钮用 `opacity-0 group-hover:opacity-100`，触屏无稳定 hover | 移动端常显或改点击展开 |
| 前端上传校验只查扩展名 | `utils/upload.js` 仅比对后缀，重命名为 `.jpg` 的非图片文件可通过；拖拽路径按 MIME 过滤，两条路径不一致 | 统一为 MIME + 扩展名双校验 |
| ESLint / Prettier | 43 个源文件零 lint | 会引入依赖与大量风格告警，收益有限故暂缓 |

---

## 四、新增的环境变量

在 `server/.env` 中可选配置：

| 变量 | 默认值 | 说明 |
|------|--------|------|
| `BACKUP_DIR` | `server/data/backups` | 数据库归档备份目录。**建议指向另一块磁盘或异地目录** |
| `BACKUP_KEEP` | `20` | 归档备份保留份数，超出自动删除最旧的 |
| `BACKUP_TO_CLOUD` | `false` | 是否把数据库快照同步到对象存储。**开启前须确认备份前缀为私有读** |
| `BACKUP_CLOUD_PREFIX` | `backups/` | 云端备份的对象前缀，务必与照片的 `uploads/` 区分 |

原有的 `JWT_SECRET`、`PORT`、`FRONTEND_URL`、`NODE_ENV`、`KEEP_ORIGINAL` 保持不变。

---

## 五、数据文件变动

`server/data/` 下的 3 个历史手工备份已**移动**（未删除）到 `server/data/backups/`：

- `db.json.bak-1788833399626`
- `db.json.bak-before-backfill-1788850297`
- `db.json.bak-titlefix-1788862381`

如需找回，原文件仍完整保留在该目录下。

---

## 六、如何回滚

本轮改动涉及以下文件，可用 git 回滚：

```bash
git checkout -- server/ src/
rm -f .github/workflows/ci.yml src/components/NotFound.jsx
```

数据库无需回滚：新增的 `taken_at`、`password_changed_at` 字段对旧代码无影响，
被清理的黑名单条目与归档备份也不影响服务启动。

---

## 七、存储方案评估（2026-09-21）

### 当前实际用的是什么

| 层 | 方案 |
|----|------|
| 元数据（相册、照片标题/描述/尺寸/拍摄时间、管理员账号、存储配置） | **单个 JSON 文件** `server/data/db.json`，启动时整体读入内存，写操作全量重写（临时文件 + fsync + rename 原子落盘，写前双层备份） |
| 图片文件 | 本地 `server/uploads/` 或云对象存储（OSS / COS / **Kodo**），后台可切换 |
| 会话 | JWT + httpOnly Cookie，无外部依赖 |
| 外部依赖 | **无**。没有数据库服务、没有云平台账号依赖 |

### 实测：JSON 单文件在什么规模下会吃力

在本机实测（含序列化、原子写盘、两份备份复制的完整写入流程）：

| 照片数 | db.json 体积 | 单次写入耗时 | 相册查询 | 全量排序 |
|--------|-------------|-------------|---------|---------|
| 50 | 37 KB | 6.3 ms | 0.01 ms | — |
| 200 | 139 KB | 8.3 ms | 0.01 ms | — |
| 500 | 345 KB | 15.1 ms | 0.02 ms | 0.81 ms |
| 1000 | 686 KB | 23.8 ms | 0.04 ms | — |
| 2000 | 1380 KB | 36.6 ms | 0.05 ms | 3.26 ms |
| 5000 | 3460 KB | 72.7 ms | 0.12 ms | 8.56 ms |

**结论：5000 张照片时，写操作 72.7ms、读请求排序 8.56ms，都远未到需要换数据库的程度。**
且写入是同步阻塞但只发生在写操作（上传/改名/删除）时，读请求不涉及写盘。

> 此前估计的「2000 张开始有感」偏保守，实测 2000 张仅 36.6ms。

### SQLite 与 Supabase 的取舍

**结论：现在都不迁。** 若将来确实要迁，**SQLite 是唯一合理选择**，理由：

| 维度 | SQLite | Supabase |
|------|--------|----------|
| 成本 | 0 | 免费额度有限，且**项目不活跃会被自动暂停** |
| 数据位置 | 本机 | 海外机房，涉及**数据出境** |
| 依赖 | 无 | 依赖外网与第三方服务可用性——它一挂，整站打不开 |
| 延迟 | 本地、微秒级 | 每次查询多一个网络往返（50–200ms） |
| 迁移成本 | 小（只改 `db.js`，接口层不动） | 大（数据层 + 网络层 + 错误处理全要改） |
| 适合场景 | 单机自托管 | **多端同步、需要用户体系** |

Supabase 的价值在于多端实时同步与内置用户系统。本项目是**单管理员自用**，
不需要这些；而它带来的「数据出境」与「多一个跨国云依赖」恰好踩中你在意的两点。
更关键的是：把元数据放云端、图片放七牛，等于把一个本地小项目拆成两个云依赖。

### 什么时候再考虑迁移

出现以下任一情况时，迁 SQLite：

- 单次写入延迟超过 **200ms**（按实测曲线约 15000 张照片）
- 需要全文搜索、按日期/地点多条件组合筛选
- 需要多设备实时同步或多人协作
- 需要事务保证的多表关联操作

届时的迁移成本不会变高——数据越多，迁收益越大，且接口层（`db.js` 的导出函数）可以保持不变，
路由代码基本无需改动。

---

## 八、EdgeOne 部署约束评估（2026-09-21）

前提：部署到 **EdgeOne Pages（不含中国大陆区域）**，图片存 **七牛 Kodo（新加坡）**，
沿用后台登录功能。

### 查证结论：当前的架构无法直接部署上去

| 需求 | EdgeOne 限制 | 结果 |
|------|-------------|------|
| 单张上传上限 20MB | Cloud Functions 请求体 **6MB** | ❌ **上传功能直接不可用** |
| 单次最多 20 张（理论 400MB） | 同上 | ❌ 同上 |
| `server/data/db.json` 持久化 | Serverless **无持久文件系统** | ❌ 每次冷启动文件都消失 |
| 用 KV 存数据 | **KV 仅 Edge Functions 可用，Cloud Functions 不支持** | ❌ 不可用 |
| sharp 图片处理 | 官方明确支持 sharp / Jimp / FFmpeg | ✅ 可行 |
| 后台登录（会话 + 用户数据） | 需外部持久化 | ⚠️ 依赖外部数据库 |

其他限制：Cloud Functions 代码包 128MB（够用）、墙钟上限 120s（默认 30s，需在
`edgeone.json` 配 `maxDuration`）、运行区域可指定 `overseasRegions`（默认新加坡）。

### Blob 存储能当数据库用吗

EdgeOne 提供 Blob（分布式对象存储，Cloud Functions 可用，单值上限 25MB），
官方用例包含「按目录组织的结构化数据集（多个 JSON 文件）」。

**但它是对象存储，不是数据库**：没有事务、没有并发写控制、每次读写都是一次网络往返。
把现有的「JSON 单文件全量重写」搬到 Blob 上，等于把本地内存操作换成网络操作，
且失去原子性保证——**不推荐**用于相册/照片/用户这类需要关联查询的数据。

### 数据库选型（在全 Serverless 前提下）

| 方案 | 评价 |
|------|------|
| **Supabase** | EdgeOne 官方集成之一。Postgres + Auth + Storage，新加坡区与部署区域同区。**⚠️ 免费版不活跃会被暂停**——照片站可能数日无访问，一暂停整站打不开；正价约 $25/月起 |
| PostgreSQL / MySQL | 最标准，但需自行运维一个数据库实例 |
| Turso（libSQL） | SQLite 的边缘 HTTP 版本，最贴合本项目的数据模型，免费额度大方 |
| Blob 存 JSON | 见上，不推荐 |

> **修正此前的判断**：在第 4 轮的评估里，结论是「Supabase 不合适」。
> 那个结论的**前提是「后端跑在自己的服务器上」**——自家服务器上有持久文件系统，
> 本地 SQLite 显然优于外部云数据库。
> 若改为全 Serverless 部署，前提不再成立（没有文件系统），
> **Supabase 就变成一个合理选项了**。结论随前提变化，特此更正。

### 两条可行路线

**路线 A：EdgeOne 托管前端 + 后端跑在一台轻量服务器**（推荐）

- 前端静态资源交给 EdgeOne 的全球 CDN（这恰是它的强项，且免费额度宽松）
- 后端 Express 保持现状（可写文件系统、20MB 上传、sharp 处理、JSON/SQLite 存储）
- **现有代码几乎无需改动**，`DEPLOY.md` 里的 VPS 方案可直接用
- 数据库继续用 JSON（实测 5000 张仍充裕），零外部依赖、零成本
- 代价：需要一台服务器（最便宜的云主机约 ¥10–30/月，或先用现有机器）

**路线 B：全 Serverless**

必须改造两处：
1. **上传改为前端直传七牛**——后端只签发上传凭证，前端直传 Kodo，绕过 6MB 限制；
   缩略图改用七牛自带的图片处理（`imageView2` / `imageMogr2`），可完全替代 sharp，
   且带 CDN 缓存，比服务器实时处理更快
2. **元数据改存外部数据库**——Supabase / Turso / 自建 Postgres 三选一

代价：改造量不小（前端上传链路重写 + 数据层重写），且引入外部依赖
（云数据库不可用时整站打不开）。

### 建议

**推荐路线 A。** 理由：改动最小、上传与图片处理不受限、数据留在自己的磁盘上、
零外部依赖、成本可控。EdgeOne 的价值在于静态资源全球加速，用它托管前端即可，
没必要为了「全 Serverless」把两个成熟模块（文件写入、本地图片处理）推翻重写。

若确实要求全 Serverless，则按路线 B 改造，数据库优先考虑 **Turso**（贴合 SQLite 心智），
其次 Supabase（生态成熟但需接受可能的休眠）。

> ⚠️ 用户明确选择**全 Serverless**（路线 B）。实际实施结果见下节。

---

## 九、EdgeOne 全 Serverless 改造（已实施）

用户决定：前端、后端、数据全部部署在 EdgeOne Pages（不含中国大陆区域）。
以下为**已完成的改造**，部署与配置说明见 **[DEPLOY-EDGEONE.md](./DEPLOY-EDGEONE.md)**。

### 数据库最终选型：libSQL / Turso

选它而非 Supabase 的决定性原因：

1. **无休眠问题** —— Supabase 免费版项目不活跃会被暂停，而照片站可能数日无访问，一旦暂停整站打不开。
   用 Turso 就不需要额外搞一个定时任务去保活
2. **数据模型贴合** —— 本项目表结构本就是 SQLite 风格，SQL 无需改写
3. **本地零配置** —— 同一套代码用 `file:` 指向本地文件即可开发，**不需要注册任何云账号**
   就能跑通全部业务逻辑；生产只换连接串
4. **不会被锁死** —— libSQL 开源，数据随时可导出为标准 SQLite 文件

### 已完成的改造清单

| # | 改动 | 说明 |
|---|------|------|
| 25 | 新建 EdgeOne 后端 | `cloud-functions/api/[[default]].js`：ESM Express 单文件自包含应用，接管 `/api/*` |
| 26 | 数据层换为 libSQL | 全部 SQL 化实现，保持原 `db.js` 的函数语义（排序规则、软删除、封面回退等行为一致） |
| 27 | 上传改为浏览器直传七牛 | 三步链路：取凭证 → 直传 Kodo → 提交元数据。**绕开 6MB 请求体限制** |
| 28 | 上传凭证自行签名 | 用 `node:crypto` 实现七牛 HMAC-SHA1 签名，**不引入七牛 SDK**（减小体积、无 CJS 互操作问题） |
| 29 | 缩略图改由七牛 imageView2 生成 | 服务端不再需要 sharp；数据库只存对象 key，URL 响应时计算 —— 调整尺寸/质量**无需迁移数据** |
| 30 | 客户端读取宽高与拍摄时间 | 新增 `src/utils/photoMeta.js`；图片不再经过服务端，这两项必须在浏览器取 |
| 31 | 移除「对象存储」管理页 | 七牛凭证改由环境变量提供，不再是运行时配置（Serverless 的 12-factor 做法） |
| 32 | 新增 `edgeone.json` | 区域 `ap-singapore`、`maxDuration: 60`、静态资源缓存头、安全响应头 |
| 33 | 新增本地开发入口 | `cloud-functions/dev-server.mjs`，无需任何云服务即可跑通全链路 |
| 34 | 双重挂载 API 路由 | 兼容平台「是否剥离 `/api` 前缀」两种行为；**刻意不改写 `req.url`**（改写会把前端路由误判成接口路径） |

### 关键技术决策与踩坑

| 问题 | 处理 |
|------|------|
| EdgeOne 的 `cloud-functions/` 下每个 `.js` 都会成为路由 | 后端写成**单个自包含文件**，辅助模块不放在该目录 |
| `[[` `]]` 出现在文件名里，无法直接 import | 本地开发用 `pathToFileURL()` 转换后再动态导入 |
| libSQL 0.14+ 的 `createClient` 只接受对象参数 | 已按新签名调用（旧写法会报 "Expected client configuration as object"） |
| 生产走纯 JS、本地需原生模块 | 按 URL 协议动态选择 `@libsql/client/web`（远程）或 `@libsql/client`（本地 file:） |
| `trust proxy: true` 被 express-rate-limit 判定为可绕过 | 改为数值跳数 `1`（可用 `TRUST_PROXY_HOPS` 调整） |
| exifr 被打进公开访客加载的主包（+73KB） | 改为动态 import，拆成独立 chunk（73.5KB），主包回到 428KB |
| 平台是否剥离 `/api` 前缀未知 | 双重挂载同时覆盖两种情况 |

### 验证结果

本地端到端 **46/46 通过**（真实 Express + 真实 SQLite + 完整业务链路）：
认证与权限、直传凭证签发（含 scope/insertOnly/fsizeLimit/mimeLimit 校验）、
伪造 key 拦截、元数据入库、URL 计算（含 GIF 例外）、排序、封面回退、
软删除/恢复/彻底删除、字段白名单、改密码全端下线、路由回退、无未捕获异常。

前端单元测试 19/19，构建通过。

### 尚未验证的部分（重要）

以下属于平台集成行为，**只能在真正部署到 EdgeOne 后确认**：
`/api` 前缀行为、SPA 回退、环境变量注入方式、`@libsql/client` 原生模块打包、冷启动耗时。
详见 DEPLOY-EDGEONE.md 第十节。

### 旧代码处置

`server/` 目录**完整保留、不再维护**，供对照与回退使用。
改造前的数据无需迁移（当时库中为空：0 相册 0 照片）。

---

## 十、函数部署区改为东京（2026-09-22）

| # | 类别 | 改动 | 状态 | 验证方式 |
|---|------|------|------|----------|
| 35 | 性能/运维 | EdgeOne 函数部署区 `ap-singapore` → `ap-tokyo`，与 Turso 同城 | ✅ 已完成（待部署后确认） | 官方文档核对 + 本地实测库连通 7/7 |

### 起因：Turso 实际只能建在东京

第八节的部署说明原本写「location 必须指定新加坡（sin），与 EdgeOne 函数、七牛桶同区」。
**该前提不成立**：实际账号下 Turso 可选区域**只有东京，其余全部在美国**，新加坡建不了。
而线上库已经是东京（`aws-ap-northeast-1`）。

因此问题从「把数据库挪到新加坡」变成「**把函数挪到东京**」。

### 为什么函数该跟数据库同区，而不是跟七牛桶同区

先确认了函数到底跟谁打交道（读 `[[default]].js` 的网络调用）：

| 函数与外部对象的交互 | 是否有网络请求 | 频次 |
|---------------------|--------------|------|
| 读写 Turso（元数据） | ✅ 每次 API 请求 | **高** |
| 签发七牛上传凭证（`createUploadToken`） | ❌ 纯本地 `HMAC-SHA1` | — |
| 计算图片展示 URL | ❌ 纯字符串拼接 CDN 域名 | — |
| 上传图片本体 | 浏览器**直传七牛**，不经过函数 | — |
| 展示图片 | 走 CDN（`img.iceuu.icu`），不经过函数 | — |
| 彻底删除时移除云端对象 | ✅ `fetch('https://rs.qiniuapi.com...')` | **低**（仅彻底删除） |

**结论：函数唯一的高频外部依赖是数据库。** 七牛桶在新加坡与否，对函数几乎无影响——
只有「彻底删除」那一次调用，且低频、延迟不敏感。
所以函数与 **Turso** 同区（东京），比与七牛桶同区更有价值。

### 改动内容

- `edgeone.json` — `overseasRegions: ["ap-singapore"]` → `["ap-tokyo"]`
- `DEPLOY-EDGEONE.md` — 更正区域说明（含被推翻的旧结论）、架构图、部署步骤与验证清单

### 收益

省掉每次数据库读写的跨国往返。原配置下函数在新加坡、库在东京，
**每个 API 请求**都要多一次约 70–90ms 的跨国 RTT；而后台上传一张照片要写好几次库，会累加。

> 顺带收益：访客（华东）到东京比到新加坡更近，API 响应也更快。

### 验证

- **本地实测（2026-09-22）**：直连该 Turso 库确认表结构与数据；
  用真实 Turso 跑后端全链路 **7/7 通过**（健康检查、登录、会话校验、相册列表、公开接口、
  取上传凭证的明确报错、错误密码 401）
- 本机（福建）→ 东京库单次往返约 **193–200ms**
- **待部署后确认**：函数实际运行区域、函数↔库的实际延迟（见 DEPLOY-EDGEONE.md 第十节）

### 未受影响的项

`QINIU_REGION=as0`（新加坡）**保持不动**——桶的地理位置没变，
它只决定表单上传域名（`upload-as0.qiniup.com`）与 `imageView2` 的处理节点。

---

## 十一、当前推进状态（2026-09-22 审查）

代码侧基本完工，卡在外部环节。以下为审查实测所得。

### 已实测通过

| 项 | 结果 |
|----|------|
| 前端构建 | 通过 |
| 前端单元测试 | **20/20**（此前文档记 19/19，已更正） |
| 新版后端（本地 SQLite） | **15/15** |
| 新版后端（真实 Turso） | **7/7** |
| Turso 表结构 | users 1 / albums 0 / photos 0 / token_blacklist 2 |

### 阻塞项

| # | 阻塞项 | 现状 |
|---|--------|------|
| 1 | ~~七牛凭证未配置~~ | **已配置**（2026-09-22）。配置后暴露出一个真实缺陷（见第十二节），已修复并跑通完整链路 |
| 2 | ~~代码未推送~~ | **已推送**（2026-09-22）。远端 `main` = `88a11c1`，GitHub Actions CI 通过 |
| 3 | 域名无解析 | `iceuu.icu` 返回 `ENOTFOUND`，站点未上线（`img.iceuu.icu` 已可用） |

### 欠账

- `scripts/migrate-to-turso.mjs` 处于**未跟踪**状态，未纳入版本控制
- 「46 项端到端验证」的脚本**未留在仓库**；新版 `tests/` 仅前端 3 个文件，
  后端改动缺少可重复运行的自动化测试（`server/tests/` 下有测试，但属已冻结的旧版）
- EdgeOne 控制台的环境变量尚未更新（`TURSO_AUTH_TOKEN` 已在本地轮换）

---

## 十二、base64url 剥离填充导致七牛签名全部失效（缺陷修复 · 严重，2026-09-22）

| # | 类别 | 改动 | 状态 | 验证方式 |
|---|------|------|------|----------|
| 36 | **缺陷** | `b64url()` 不再剥离 `=` 填充 | ✅ 已完成 | 真实直传：官方 SDK 交叉验证 + 全链路 7/8（1 项为 CDN 缓存误判，已复核为通过） |

### 问题

`cloud-functions/api/[[default]].js` 的 `b64url()` 按 JWT 的习惯剥掉了 base64 的 `=` 填充：

```js
Buffer.from(input).toString('base64').replace(/\+/g, '-').replace(/\//g, '_').replace(/=+$/, '')
//                                                                          ^^^^^^^^^^^^^^^^ 元凶
```

但七牛要求的是「**只把 `+` `/` 换成 `-` `_`**」的 urlsafe base64，**填充必须保留**——
官方 SDK 的 `util.base64ToUrlSafe()` 就不删 `=`。

**影响范围：七牛所有签名路径，全部失效**
- `createUploadToken()` → 上传凭证无效 → **任何照片都传不上来**
- `deleteQiniuObject()` → 删除签名无效 → **彻底删除时云端对象删不掉**

### 为什么错误信息极具误导性

七牛对无效签名一律返回：

```
401 {"error":"bad token","error_code":"BadToken"}
```

「bad token」听起来像密钥不对，于是很容易往「AK/SK 配错了」的方向排查。
实测确认：**该账号的 AK/SK 完全有效**（用官方 SDK 生成的凭证直传返回 `HTTP 200`）。

### 定位方法（可复用）

不看错误信息猜，而是**用官方 SDK 做交叉验证**。项目里 `server/node_modules/qiniu`
（7.15.2）就能直接用：

| 对比项 | 自实现 | SDK | 结论 |
|--------|--------|-----|------|
| HMAC 签名 | `UGUg8VHVMWzOzozx7p39sDRQC2I` | `UGUg8VHVMWzOzozx7p39sDRQC2I=` | **差一个 `=`** |
| 管理凭证 | `QBox AK:Ce-pcvhmhzhZmw1dlraOyqkjCeE` | `QBox AK:Ce-pcvhmhzhZmw1dlraOyqkjCeE=` | 同上 |

再用同一个不存在的对象做 `stat`，一次就能判定：

| 签名变体 | 七牛响应 | 含义 |
|---------|---------|------|
| 去掉 `=`（改前） | `401 bad token` | 签名被拒 |
| 保留 `=`（改后） | `612 no such file` | **签名有效**，只是文件不存在 |

### 修复

```js
function b64url(input) {
  return Buffer.from(input).toString('base64').replace(/\+/g, '-').replace(/\//g, '_')
}
```

已加注释说明「不可删填充」，避免以后有人按 JWT 习惯改回去。

### 验证（真实七牛，非 mock）

| 步骤 | 结果 |
|------|------|
| ① 取上传凭证 | ✅ |
| ② 浏览器同路径直传（Node 原生 `fetch` + `FormData`） | ✅ 返回 `hash` |
| ③ 元数据入库 | ✅ |
| ④ 缩略图 URL 可访问 | ✅ `200 image/webp` |
| ⑤ 展示图 URL 可访问 | ✅ `200` |
| **`imageView2` 生效** | ✅ 返回 `image/webp`，说明图片处理与 CDN 均正常 |
| ⑥ 彻底删除（含云端对象） | ✅ 收尾 `stat` 返回 `612`，对象确已从桶中移除 |

> **一个容易误判的点**：彻底删除后 CDN 仍可能返回 200（缓存未过期），
> 这不代表删除失败。要确认用管理接口 `stat` 查对象是否存在，别只看 CDN。

> **另需注意**：本次改造的 46 项端到端验证**没有覆盖真实七牛调用**（凭证为 mock），
> 所以这个缺陷当时没被发现。凡涉及第三方签名的代码，必须有至少一次真实调用验证。

### 未修的相关问题（见下节）

`src/api/real.js` 的直传步骤**没有重试**。实测 `upload-as0.qiniup.com` 的 HTTPS
连通性很差（见下），直传会间歇失败并直接计入 `failed`，建议加有限次重试。

---

## 十三、上传节点连通性实测（2026-09-22，网络层面）

配置好凭证后实测从本机（福建）到七牛各节点的表现：

| 目标 | 结果 |
|------|------|
| `upload.qiniup.com`（国内华东） | TCP 31ms，**快且稳** |
| `rs.qiniuapi.com`（管理接口，国内） | TCP 34ms，稳 |
| `upload-as0.qiniup.com`（新加坡） | **TLS 握手 10 次仅 5 次成功**，成功时也要 2–8.4 秒 |
| `img.iceuu.icu`（绑新加坡桶） | 单次请求 237ms–3.7s，偶发超时 |

**这是「用海外区域换免备案」的代价**：国内↔新加坡链路在晚高峰明显拥堵。

对本项目的影响：
- **上传**会间歇失败（浏览器直传无重试 → 用户看到「上传失败」）
- **图片加载**偏慢（前台瀑布流一屏几十张图，体感会明显）

可选对策（尚未实施，待用户决策）：
1. 给直传加有限次重试 + 失败提示（改动最小，建议先做）
2. 换国内区域桶 —— 速度最好，但**必须 ICP 备案**，与既定合规要求冲突
3. 咨询七牛是否有海外上传加速方案
